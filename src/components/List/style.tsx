import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    boxSigno: {
        padding: 10,
        borderWidth:1,
        borderColor: '#000',
        borderRadius: 2,
        marginBottom: 10
    },
    nameSigno: {
        fontSize: 18,
        marginBottom: 5
    }
});

export default style;